langauge: java
jdk:
  - openjdk8

branches:
  only:
    - master

cache:
  directories:
    - '$HOME/.m2'

before_install:
  - chmod +x mvnw

script: "./mvnw clean install"

notifications:
  email:
    recipients:
      - dasd412@naver.com
